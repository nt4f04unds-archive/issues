name: audio_service_834
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  just_audio: 0.7.4
  audio_service:
    ^0.18.0
    # path: C:/dev/projects/native/audio_service/audio_service
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
